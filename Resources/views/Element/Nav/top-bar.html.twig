<!-- topbar starts -->
<div id="sk-topBar" class="closed">
    <div id="skMenuOpener">
        <a href="#"><i class="fa fa-arrow-left"></i></a>
    </div>
    <div class="navbar-inner">
        <div class="skcontainer-fluid">

            <div class="">
                <a href="{{path('sk_admin_homepage')}}" class="menuLink"><span class="semi-big"><strong>SOLID</strong>KISS </span></a>
            </div>
            <div class="top-nav ">
                <ul id="sk-topBarNav">
                    <li class=""><a href="{{websitePath}}" class="menuLink"><i class="fa fa-eye"></i> view site</a></li>
                    <li class="sk-dropdown" data-target="dropdown-create"><a href="#" data-toggle="dropdown" class="menuLink"><i class="fa fa-paint-brush"></i> Create</a>
                        <ul class="sk-dropdown-menu" id="dropdown-create">
                            {#                                            {% for group in menuGroups%}#}
                            {# <li class="nav-header hidden-tablet">{{group.name}}</li>
                             <li class="divider"></li>#}
                            {% for elementType in entititesType%}
                                {#                                                   {% if elementType.menuGroup == group.id%}#}
                                {#                                                   <li>{{elementType}}</li>#}
                                <li><a class="" href="{{path('sk_admin_edit',{'entity':elementType.name,'_locale':app.request.locale})}}"><span class="hidden-tablet"> {{elementType.beautyName|title}}</span></a></li>
                                {#                                                               <li class="divider"></li>#}
                                {#                                                   {%endif%}#}
                            {%endfor%}
                            {#                                           {%endfor%}#}
                            {#<li><a href="#">Profile</a></li>
                            <li class="divider"></li>
                            <li><a href="login.html">Logout</a></li>#}
                        </ul>
                    </li>

                    {%if editPath is not null%}
                        <li class=""><a href="{{editPath}}" class="menuLink"><i class="fa fa-edit"></i> Edit Entity</a></li>
                    {%endif%}

                    {%if modules.contact.enabled == true %}
                        <li class=""><a href="{{path('sk_admin_contactlist')}}" class="menuLink"><i class="fa fa-envelope"></i> Contact ({{newContactMessageNumber}})</a></li>
                    {%endif%}
                    {# <li>
                                                                    <form class="navbar-search pull-left">
                                                                            <input placeholder="Search" class="search-query span2" name="query" type="text">
                                                                    </form>
                                                            </li>#}
                    <!-- user dropdown starts -->
                    <li>
                        <div class="btn-group" >
                            <a class="btn sk-dropdown" id="userbox" data-target="dropdown-user" href="#">
                                <i class="fa fa-user"></i><span class="hidden-phone"> {{app.user.username}}</span>
                                <span class="caret"></span>
                            </a>
                            <ul id="dropdown-user" class="sk-dropdown-menu">
                                <li><a href="{{path('sk_admin_useredit',{id:app.user.id})}}">{{'Profil'|trans()}}</a></li>
                                <li class="divider"></li>
                                <li><a href="{{path('fos_user_security_logout')}}">{{'Deconnexion'|trans()}}</a></li>
                            </ul>
                        </div>
                    </li>
                    <!-- user dropdown ends -->
                </ul>
            </div><!--/.nav-collapse -->
        </div>
    </div>
</div>
