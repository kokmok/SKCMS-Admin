{%extends "SKCMSAdminBundle:Element/Template:admin-list.html.twig"%}

{#{%block adminContent%}#}

{#<a href="{{path('sk_admin_edit',{entity:entityParams.name})}}" class="btn btn-large btn-primary">Créer {{entityParams.beautyName}}</a>#}

{%block header2 %}Blog posts {{parent()}}{%endblock%}

{%block th %}
<th>#</th>
<th>Date</th>
<th>Titre</th>
<th>Categorie</th>
              
{{parent()}}
{%endblock%}

{%block tr %}
    <td  class="data-sort" data-sort="desc">{{entity.id}}</td>
    <td>{{entity.creationDate|date('d/m/Y H:i:s')}}</td>
    <td>{{entity.title}}</td>
    <td>{{entity.category}}</td>

    <td class="center">
        <a class="btn btn-info" href="{{path('sk_admin_blogpostedit',{id:entity.id})}}">
                <i class="icon-edit icon-white"></i>  
                Edit                                            
        </a>
        <a class="btn btn-danger btn-setting" data-content="Voulez vous vraiment supprimer cette entité ?" data-title="Confirmation de suppression" data-confirm="Oui" href="{{path('sk_admin_blogpostdelete',{id:entity.id})}}">
                <i class="icon-trash icon-white"></i> 
                Delete
        </a>

    </td>
{%endblock%}

{%block createLink %}{{path('sk_admin_blogpostedit')}}{%endblock%}

